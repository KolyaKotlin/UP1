<Page x:Class="_122_Fedchenko.Pages.AddPaymentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="AddPaymentPage">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Название платежа:"
                       Grid.Row="0"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="0,5" />
            <TextBox x:Name="TBPaymentName"
                     Grid.Row="0"
                     Grid.Column="1"
                     Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5"
                     MinWidth="200" />

            <TextBlock Text="Сумма:"
                       Grid.Row="1"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="0,5" />
            <TextBox x:Name="TBAmount"
                     Grid.Row="1"
                     Grid.Column="1"
                     Text="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5" />

            <TextBlock Text="Количество:"
                       Grid.Row="2"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="0,5" />
            <TextBox x:Name="TBCount"
                     Grid.Row="2"
                     Grid.Column="1"
                     Text="{Binding Num, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5" />

            <TextBlock Text="Дата:"
                       Grid.Row="3"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="0,5" />
            <TextBox x:Name="TBDate"
                     Grid.Row="3"
                     Grid.Column="1"
                     Text="{Binding Date, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=dd.MM.yyyy}"
                     Margin="0,5" />

            <TextBlock Text="Пользователь:"
                       Grid.Row="4"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="0,5" />
            <ComboBox x:Name="CBUser"
                      Grid.Row="4"
                      Grid.Column="1"
                      DisplayMemberPath="FIO"
                      SelectedValuePath="ID"
                      SelectedValue="{Binding UserID, Mode=TwoWay}"
                      Margin="0,5"
                      MinWidth="200" />

            <TextBlock Text="Категория:"
                       Grid.Row="5"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="0,5" />
            <ComboBox x:Name="CBCategory"
                      Grid.Row="5"
                      Grid.Column="1"
                      DisplayMemberPath="Name"
                      SelectedValuePath="ID"
                      SelectedValue="{Binding CategoryID, Mode=TwoWay}"
                      Margin="0,5"
                      MinWidth="200" />

            <StackPanel Grid.Row="6"
                        Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,15,0,0">
                <Button Content="Сохранить"
                        Width="100"
                        Margin="10"
                        Click="ButtonSave_Click" />
                <Button Content="Очистить"
                        Width="100"
                        Margin="10"
                        Click="ButtonClean_Click" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
